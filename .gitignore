# --- DEPENDENCIES ---
/node_modules
/vendor

# --- LARAVEL FRAMEWORK ---
.env
.env.backup
.env.production
.phpunit.result.cache
/.phpunit.cache
/storage/*.key
/storage/pail
/auth.json

# --- SYSTEM & IDE ---
.DS_Store
Thumbs.db
*.log
/.fleet
/.idea
/.nova
/.vscode
/.zed
.phpactor.json
Homestead.json
Homestead.yaml

# --- VERCEL & BUILD (MODIFIED) ---
# Folder ini biasanya di-ignore, tapi Anda membukanya agar aset ter-upload
/public/hot

# PERBAIKAN VERCEL: Kami biarkan ini dikomentari (ada tanda #) 
# agar CSS/JS hasil 'npm run build' di laptop Anda ikut ter-upload.
# /public/build

# PERBAIKAN VERCEL: Kami biarkan ini dikomentari (ada tanda #)
# agar symlink atau file gambar di storage ikut ter-upload.
# /public/storage

# --- CRITICAL FIX FOR ERROR 500 ---
# Bagian ini WAJIB ada untuk mencegah error "View not found" di Vercel.
# Kita ignore semua file cache, TAPI tetap mempertahankan file .gitignore di dalamnya.
/bootstrap/cache/*
!/bootstrap/cache/.gitignore